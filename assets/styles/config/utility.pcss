@utility container {
  margin-inline: auto;
  padding-inline: 1rem;
}

/**
 * define class `animate`, which can used eyery where
 */

@utility animate {
  @apply transition duration-500 ease-in-out;
}

@utility translate-x-half {
  transform: translateX(-50%);
}
